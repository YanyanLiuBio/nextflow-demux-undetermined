docker {
    enabled = true
}

process {


    withName: DEMUX {
        container = 'seqwell/fastq_demux:master'
        containerOptions = '--entrypoint=""'
        executor = 'local'
        cpus = 12
        memory = '60 GB'
    }
}